<%= form_with(model: board, local: true, class: "form") do |f| %>
    <%= f.text_field :name, required: true, placeholder: "Board Name:", class: "name-field" %>
    <%= f.submit "Submit", class:"submit"%>
<% end %>